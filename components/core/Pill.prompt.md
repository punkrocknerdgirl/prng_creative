Filled blush keyword chip — group several in a wrap row under a card body to tag capabilities.

```jsx
<div style={{display:'flex',flexWrap:'wrap',gap:'0.35rem'}}>
  <Pill>QBO</Pill><Pill>Month-End Close</Pill><Pill>Reporting</Pill>
</div>
```

For outlined credential badges use `Badge`; for status (positive/negative) use `StatusTag`.
