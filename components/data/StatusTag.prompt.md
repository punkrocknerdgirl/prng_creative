Square mono status chip — project log states (`active`/`building`/`parked`) and report reads (`positive`/`negative`/`warning`/`info`).

```jsx
<StatusTag status="active" />
<StatusTag status="negative">Net Loss</StatusTag>
```
