Text field with mono uppercase label and a pink focus ring. Sharp 2px corners.

```jsx
<Input label="Email" type="email" placeholder="ernie@prngbooks.com" />
<Input label="Company" hint="Legal entity name" />
```

Pair with `Select` and `Button` for forms.
