Native select matching `Input` — mono label, pink focus ring.

```jsx
<Select label="Service" options={['Bookkeeping','Payroll','Systems & Automation']} />
```

Pass `options` as strings or `{value,label}` objects, or supply `<option>` children directly.
